export interface IUsers {
  id: string,
  name: string,
  email: string,
  password: string,
  createdAt: string
}

export interface IProducts {
  id: string
  name: string
  price: number
  description: string
  imageUrl: string
}